# B-06: My Bookings Page

## What it does
Shows all booking requests for the user's family, sorted by check-in date
(upcoming first, then past). Each row shows a status badge and links to the
detail page.

## UI
`/dashboard/bookings` — server-rendered table.

Status badges: `pending` (yellow), `confirmed` (green), `rejected` (red),
`cancelled` (gray).
